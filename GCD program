#include <stdio.h>

int main()
{
    int a,b,rem,q;
    scanf("%d%d",&a,&b);
    while(b!=0)
    {
        rem=a%b;
        q=a/b;
        a=b;
        b=rem;
    }
    printf("%d",a);

    return 0;
}
